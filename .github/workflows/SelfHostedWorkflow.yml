name: first workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: self-hosted
    steps:
      - name: Print Hello
        run: echo "Hello World"
      - name: Print bye
        run: echo "bye bye"
